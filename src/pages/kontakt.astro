---
import Layout from '../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
import { SITE } from '../site';
---

<Layout
  title="Kontakt — Erstgespräch mit Joshua Heller"
  description="30 Minuten Erstgespräch. Unverbindlich, vertraulich, ohne Sales-Pitch. Du erzählst, was anliegt. Ich sage, ob und wie ich helfen kann."
  path="/kontakt"
  lang="de"
  alternatePath="/en/contact"
>
  <section class="bg-blobs">
    <div class="container-page py-16 md:py-20">
      <h1 class="text-4xl font-extrabold tracking-tight text-ink md:text-5xl">Lass uns sprechen.</h1>
      <p class="mt-6 max-w-2xl text-lg text-body">
        30 Minuten Erstgespräch. Unverbindlich, vertraulich, ohne Sales-Pitch. Du erzählst, was anliegt. Ich sage, ob
        und wie ich helfen kann.
      </p>
    </div>
  </section>

  <section class="container-page grid gap-12 py-12 lg:grid-cols-[1fr_22rem]">
    <div>
      <form id="contact-form" class="space-y-5" novalidate>
        <div class="grid gap-5 sm:grid-cols-2">
          <label class="block">
            <span class="text-sm font-medium text-ink">Name *</span>
            <input required name="name" type="text" class="mt-1 w-full rounded-xl border border-line bg-white px-4 py-2.5 text-body outline-none focus:border-electric" />
          </label>
          <label class="block">
            <span class="text-sm font-medium text-ink">E-Mail *</span>
            <input required name="email" type="email" class="mt-1 w-full rounded-xl border border-line bg-white px-4 py-2.5 text-body outline-none focus:border-electric" />
          </label>
          <label class="block">
            <span class="text-sm font-medium text-ink">Firma</span>
            <input name="company" type="text" class="mt-1 w-full rounded-xl border border-line bg-white px-4 py-2.5 text-body outline-none focus:border-electric" />
          </label>
          <label class="block">
            <span class="text-sm font-medium text-ink">Rolle</span>
            <input name="role" type="text" class="mt-1 w-full rounded-xl border border-line bg-white px-4 py-2.5 text-body outline-none focus:border-electric" />
          </label>
        </div>
        <label class="block">
          <span class="text-sm font-medium text-ink">Worum geht's?</span>
          <textarea name="message" rows="4" maxlength="500" class="mt-1 w-full rounded-xl border border-line bg-white px-4 py-2.5 text-body outline-none focus:border-electric"></textarea>
        </label>
        <label class="block">
          <span class="text-sm font-medium text-ink">Wo hast du von mir erfahren?</span>
          <select name="source" class="mt-1 w-full rounded-xl border border-line bg-white px-4 py-2.5 text-body outline-none focus:border-electric">
            <option>LinkedIn</option>
            <option>Empfehlung</option>
            <option>Google</option>
            <option>Sonstiges</option>
          </select>
        </label>
        <p class="text-xs text-muted">
          Mit dem Absenden öffnet sich dein E-Mail-Programm mit einem fertigen Entwurf an mich — es wird kein externer
          Dienst eingebunden. Grundlage der Verarbeitung: Art. 6 Abs. 1 lit. b DSGVO. Details in der
          <a href="/datenschutz" class="text-electric">Datenschutzerklärung</a>.
        </p>
        <button type="submit" class="btn btn-primary">Anfrage als E-Mail öffnen →</button>
      </form>

      <div id="contact-fallback" class="mt-5 hidden rounded-xl border border-line bg-surface-2 p-5 text-sm text-body">
        <p>Falls sich kein E-Mail-Programm öffnet: Entwurf kopieren und an <strong class="text-ink">{SITE.email}</strong> senden.</p>
        <button id="contact-copy" type="button" class="btn btn-ghost mt-3">Entwurf kopieren</button>
        <span id="contact-copied" class="ml-3 hidden text-electric">Kopiert ✓</span>
      </div>
    </div>

    <aside class="space-y-6">
      <div class="card-line p-6">
        <p class="font-bold text-ink">Lieber erst schreiben?</p>
        <a href={`mailto:${SITE.email}`} class="mt-2 inline-flex items-center gap-2 text-sm font-medium text-electric">
          <Icon name="lucide:mail" class="size-4" /> {SITE.email}
        </a>
      </div>
      <div class="card-line p-6">
        <p class="font-bold text-ink">Oder per LinkedIn</p>
        <a href={SITE.linkedin} target="_blank" rel="noopener" class="mt-2 inline-flex items-center gap-2 text-sm font-medium text-electric">
          <Icon name="lucide:linkedin" class="size-4" /> heller-joshua
        </a>
      </div>
      <div class="card-line p-6">
        <p class="font-bold text-ink">Standort & Aktionsradius</p>
        <p class="mt-2 text-sm text-body">
          Karlsruhe & Pforzheim · Großraum Stuttgart · Baden-Württemberg<br />Aktionsradius DACH · primär remote
        </p>
      </div>
    </aside>
  </section>
</Layout>

<script define:vars={{ email: SITE.email }}>
  const form = document.getElementById('contact-form');
  const fallback = document.getElementById('contact-fallback');

  function buildDraft() {
    const f = new FormData(form);
    const lines = [
      `Name: ${f.get('name') || ''}`,
      `E-Mail: ${f.get('email') || ''}`,
      `Firma: ${f.get('company') || ''}`,
      `Rolle: ${f.get('role') || ''}`,
      `Quelle: ${f.get('source') || ''}`,
      '',
      'Worum geht’s:',
      `${f.get('message') || ''}`,
    ];
    return lines.join('\n');
  }

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    if (!form.checkValidity()) {
      form.reportValidity();
      return;
    }
    const f = new FormData(form);
    const subject = `Anfrage von ${f.get('name') || 'Website'}`;
    const href = `mailto:${email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(buildDraft())}`;
    window.location.href = href;
    fallback?.classList.remove('hidden');
  });

  document.getElementById('contact-copy')?.addEventListener('click', async () => {
    try {
      await navigator.clipboard.writeText(buildDraft());
      const ok = document.getElementById('contact-copied');
      ok?.classList.remove('hidden');
      setTimeout(() => ok?.classList.add('hidden'), 2000);
    } catch {}
  });
</script>
